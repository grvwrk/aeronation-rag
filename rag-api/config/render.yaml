services:
  - type: web
    name: rag-api

    runtime: python

    plan: free

    buildCommand: |
      pip install uv
      uv sync

    startCommand: |
      uv run uvicorn app:app --host 0.0.0.0 --port $PORT

    autoDeploy: true